// import api from '@/services/api'

const mutationTypes = {
  LOGIN: '@pufomap/UI/user/LOGIN',
  LOGOUT: '@pufomap/UI/user/LOGIN'
}

const state = {
}

const getters = {
}

const actions = {
}

const mutations = {
}

export default {
  namespaced: true,
  mutationTypes,
  state,
  getters,
  actions,
  mutations
}
